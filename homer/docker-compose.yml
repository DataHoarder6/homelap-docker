services:
  homer:
    image: b4bz/homer:latest
    container_name: homer
    restart: unless-stopped
    ports:
      - 8090:8080
    volumes:
      - ./assets:/www/assets
    networks:
      - homelab

networks:
  homelab:
    external: true
